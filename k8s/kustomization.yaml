apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: k8s-manifest-washer

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Uncomment to add common labels to all resources
# commonLabels:
#   app: k8s-manifest-washer
#   environment: production

# Uncomment to add namespace
# namespace: default

# Uncomment to customize image
# images:
#   - name: ghcr.io/<YOUR_GITHUB_USER>/k8s-manifest-washer
#     newTag: latest
